// Alertas diários desativados no ambiente Vercel (sem suporte a processos persistentes).
// Para reativar, configure um Vercel Cron Job apontando para /api/cron/alertas.
function iniciar() {}
module.exports = { iniciar };
